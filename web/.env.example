# Leave unset in development: Vite proxies /api to the .NET API (see vite.config.ts).
# Set it when the app is served from a different origin than the API.
# VITE_API_BASE_URL=https://coe-api.example.com/api

# Where the dev proxy forwards /api.
VITE_API_PROXY_TARGET=http://localhost:5080
